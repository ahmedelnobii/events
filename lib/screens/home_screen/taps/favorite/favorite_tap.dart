import 'package:flutter/material.dart';

class FavoriteTap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Center(child: Text('Favorite Tap')));
  }
}
